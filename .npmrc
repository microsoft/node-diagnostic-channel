# Optimize npm performance for CI
prefer-offline=true
audit=false
fund=false
# Use faster registry
registry=https://registry.npmjs.org/
# Cache settings
cache-min=86400
# Reduce package lock conflicts
package-lock=true
# Faster installs
prefer-dedupe=true
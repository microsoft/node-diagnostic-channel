# Optimize npm performance for CI
prefer-offline=true
audit=false
fund=false
# Use faster registry
registry=https://registry.npmjs.org/
# Reduce package lock conflicts
package-lock=true
# Faster installs
prefer-dedupe=true
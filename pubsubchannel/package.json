{
    "name": "pubsub-channel",
    "version": "0.0.1",
    "main": "./channel.js",
    "types": "./channel.d.ts",
    "scripts":{
        "prepublish": "tslint -c tslint.json -p tsconfig.json && tsc",
        "test": "tsc && mocha tests"
    },
    "dependencies": {
        "semver": "^5.3.0"
    },
    "devDependencies": {
        "typescript": "^2.2.1",
        "mocha": "^3.2.0",
        "tslint": "^5.0.0"
    },
    "license": "MIT"
}
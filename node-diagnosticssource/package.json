{
  "name": "diagnosticssource",
  "version": "0.0.1",
  "main": "./.dist/channel.js",
  "types": "./.dist/channel.d.ts",
  "scripts": {
    "build": "tsc --outDir ./.dist",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "clean": "rimraf ./.dist",
    "prepublish": "npm run build lint && npm run build",
    "test": "npm run build && mocha ./.dist/tests/**/*.js"
  },
  "dependencies": {
    "semver": "^5.3.0"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.40",
    "@types/node": "^7.0.12",
    "mocha": "^3.2.0",
    "rimraf": "^2.6.1",
    "tslint": "^5.0.0",
    "typescript": "^2.2.1"
  },
  "license": "MIT"
}
